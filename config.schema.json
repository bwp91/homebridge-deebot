{
  "pluginAlias": "Deebot",
  "pluginType": "platform",
  "singular": true,
  "headerDisplay": "Homebridge plugin to control ECOVACS Deebot devices.",
  "footerDisplay": "For help and support please visit our [GitHub Wiki](https://github.com/bwp91/homebridge-deebot/wiki).",
  "schema": {
    "type": "object",
    "properties": {
      "countryCode": {
        "title": "Country Code",
        "description": "Select your country code where your ECOVACS account is registered.",
        "type": "string",
        "required": true,
        "oneOf": [
          {
            "title": "China (CN)",
            "enum": ["CN"]
          },
          {
            "title": "Taiwan (TW)",
            "enum": ["TW"]
          },
          {
            "title": "Malaysia (MY)",
            "enum": ["MY"]
          },
          {
            "title": "Japan (JP)",
            "enum": ["JP"]
          },
          {
            "title": "Singapore (SG)",
            "enum": ["SG"]
          },
          {
            "title": "Thailand (TH)",
            "enum": ["TH"]
          },
          {
            "title": "Hong Kong (HK)",
            "enum": ["HK"]
          },
          {
            "title": "India (IN)",
            "enum": ["IN"]
          },
          {
            "title": "South Korea (KR)",
            "enum": ["KR"]
          },
          {
            "title": "United States (US)",
            "enum": ["US"]
          },
          {
            "title": "France (FR)",
            "enum": ["FR"]
          },
          {
            "title": "Spain (ES)",
            "enum": ["ES"]
          },
          {
            "title": "United Kingdom (UK)",
            "enum": ["UK"]
          },
          {
            "title": "Norway (NO)",
            "enum": ["NO"]
          },
          {
            "title": "Switzerland (CH)",
            "enum": ["CH"]
          },
          {
            "title": "Mexico (MX)",
            "enum": ["MX"]
          },
          {
            "title": "Germany (DE)",
            "enum": ["DE"]
          },
          {
            "title": "Portugal (PT)",
            "enum": ["PT"]
          },
          {
            "title": "Australia (AU)",
            "enum": ["AU"]
          },
          {
            "title": "Italy (IT)",
            "enum": ["IT"]
          },
          {
            "title": "Netherlands (NL)",
            "enum": ["NL"]
          },
          {
            "title": "Sweden (SE)",
            "enum": ["SE"]
          },
          {
            "title": "Belgium (BE)",
            "enum": ["BE"]
          },
          {
            "title": "Denmark (DK)",
            "enum": ["DK"]
          },
          {
            "title": "OTHER",
            "enum": ["OTHER"]
          }
        ]
      },
      "username": {
        "title": "ECOVACS username",
        "description": "The username for your ECOVACS account.",
        "required": true,
        "type": "string"
      },
      "password": {
        "title": "ECOVACS password",
        "description": "The password for your ECOVACS account.",
        "required": true,
        "type": "string"
      },
      "debug": {
        "title": "Debug Logging",
        "type": "boolean",
        "description": "If enabled, more information will be added to the Homebridge log.",
        "default": false
      },
      "ref": {
        "title": "Refresh Timer",
        "type": "integer",
        "default": 120,
        "minimum": 30,
        "maximum": 600,
        "description": "An interval (in seconds) in which your devices will refresh with ECOVACS."
      },
      "hideBattHumidity": {
        "title": "Hide Battery Humidity Sensor",
        "type": "boolean",
        "description": "If enabled, the humiditiy sensor showing the battery percentage will be hidden.",
        "default": false
      },
      "disablePlugin": {
        "type": "boolean"
      }
    }
  },
  "layout": [
    {
      "type": "fieldset",
      "title": "Required Settings",
      "description": "These are the basic settings that are required for this plugin to work.",
      "items": [
        "countryCode",
        "username",
        "password"
      ]
    },
    {
      "type": "fieldset",
      "title": "Optional Settings",
      "description": "A variety of optional settings for general use of this plugin.",
      "expandable": true,
      "items": [
        "debug",
        "ref",
        "hideBattHumidity"
      ]
    }
  ]
}
