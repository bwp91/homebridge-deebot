{
  "pluginAlias": "Deebot",
  "pluginType": "platform",
  "singular": true,
  "customUi": true,
  "customUiPath": "./lib/homebridge-ui",
  "headerDisplay": "<p align=\"center\"><img width=\"60%\" src=\"https://user-images.githubusercontent.com/43026681/101321841-f0eb5280-385d-11eb-8dd4-f57113f6e078.png\"></p><p align=\"center\">For help and support please visit our <a href=\"https://github.com/bwp91/homebridge-deebot/wiki\">GitHub Wiki</a>. We hope you find this plugin useful!</p>",
  "schema": {
    "type": "object",
    "properties": {
      "name": {
        "type": "string",
        "default": "Deebot"
      },
      "countryCode": {
        "title": "Country Code",
        "description": "Select your two letter country code where your ECOVACS account is registered, or select 'OTHER' if your country is not listed.",
        "type": "string",
        "required": true,
        "oneOf": [
          {
            "title": "China (CN)",
            "enum": ["CN"]
          },
          {
            "title": "Taiwan (TW)",
            "enum": ["TW"]
          },
          {
            "title": "Malaysia (MY)",
            "enum": ["MY"]
          },
          {
            "title": "Japan (JP)",
            "enum": ["JP"]
          },
          {
            "title": "Singapore (SG)",
            "enum": ["SG"]
          },
          {
            "title": "Thailand (TH)",
            "enum": ["TH"]
          },
          {
            "title": "Hong Kong (HK)",
            "enum": ["HK"]
          },
          {
            "title": "India (IN)",
            "enum": ["IN"]
          },
          {
            "title": "South Korea (KR)",
            "enum": ["KR"]
          },
          {
            "title": "United States (US)",
            "enum": ["US"]
          },
          {
            "title": "France (FR)",
            "enum": ["FR"]
          },
          {
            "title": "Spain (ES)",
            "enum": ["ES"]
          },
          {
            "title": "United Kingdom (UK)",
            "enum": ["UK"]
          },
          {
            "title": "Norway (NO)",
            "enum": ["NO"]
          },
          {
            "title": "Switzerland (CH)",
            "enum": ["CH"]
          },
          {
            "title": "Mexico (MX)",
            "enum": ["MX"]
          },
          {
            "title": "Germany (DE)",
            "enum": ["DE"]
          },
          {
            "title": "Portugal (PT)",
            "enum": ["PT"]
          },
          {
            "title": "Australia (AU)",
            "enum": ["AU"]
          },
          {
            "title": "Italy (IT)",
            "enum": ["IT"]
          },
          {
            "title": "Netherlands (NL)",
            "enum": ["NL"]
          },
          {
            "title": "Sweden (SE)",
            "enum": ["SE"]
          },
          {
            "title": "Belgium (BE)",
            "enum": ["BE"]
          },
          {
            "title": "Denmark (DK)",
            "enum": ["DK"]
          },
          {
            "title": "OTHER",
            "enum": ["WW"]
          }
        ]
      },
      "username": {
        "title": "ECOVACS Username",
        "description": "The username for your ECOVACS account.",
        "required": true,
        "type": "string"
      },
      "password": {
        "title": "ECOVACS Password",
        "description": "The password for your ECOVACS account.",
        "required": true,
        "type": "string"
      },
      "debug": {
        "title": "Debug Logging",
        "type": "boolean",
        "description": "If enabled, more information will be added to the Homebridge logs.",
        "default": false
      },
      "disablePlugin": {
        "title": "Disable Plugin",
        "type": "boolean",
        "description": "If checked, the plugin will remove all ECOVACS accessories from Homebridge and not load the plugin on Homebridge restart.",
        "default": false
      },
      "refreshTime": {
        "title": "Refresh Timer",
        "type": "integer",
        "default": 120,
        "minimum": 30,
        "maximum": 600,
        "description": "An interval (in seconds) in which your devices will refresh with ECOVACS."
      },
      "hideBattHumidity": {
        "title": "Hide Battery Humidity Sensor",
        "type": "boolean",
        "description": "If enabled, the humidity sensor showing the battery percentage will be hidden.",
        "default": false
      },
      "lowBattThreshold": {
        "title": "Low Battery Threshold",
        "type": "integer",
        "default": 20,
        "minimum": 1,
        "maximum": 100,
        "description": "The battery percentage at which Homebridge should display a low battery status."
      },
      "motionDuration": {
        "title": "Alert Notification Time",
        "type": "integer",
        "default": 30,
        "minimum": 1,
        "maximum": 120,
        "description": "The length of time (in seconds) that the motion detector in Homebridge will be activated if your Deebot sends a message or needs help. The message sent is viewable in the Homebridge logs."
      }
    }
  },
  "layout": [
    {
      "type": "fieldset",
      "title": "Required Settings",
      "description": "These are the basic settings that are required for this plugin to work.",
      "items": [
        "countryCode",
        "username",
        "password"
      ]
    },
    {
      "type": "fieldset",
      "title": "Optional Settings",
      "description": "A variety of optional settings for general use of this plugin.",
      "expandable": true,
      "items": [
        "debug",
        "disablePlugin",
        "refreshTime",
        "hideBattHumidity",
        "lowBattThreshold",
        "motionDuration"
      ]
    }
  ]
}
